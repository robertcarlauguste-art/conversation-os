# Deployment

Deployment docs (environments, secrets management, release process)
live here once ConversationOS has an actual deployment target.
Sprint 0 only covers local Docker Compose — see the root `README.md`.
