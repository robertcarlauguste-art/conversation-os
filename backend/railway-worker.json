{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": ["/backend/**"]
  },
  "deploy": {
    "startCommand": "arq app.processing.worker.WorkerSettings",
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 10
  }
}
