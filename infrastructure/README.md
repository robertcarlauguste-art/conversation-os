# infrastructure/

Reserved for infrastructure-as-code (Terraform, deployment manifests,
cloud config) once ConversationOS has an actual deployment target.
Empty in Sprint 0, which only covers local Docker Compose.
